export class AddLiftModal {
    /* @ngInject */
    constructor() {}

    $onInit() {}

    submitForm() {
        if (this.addLiftForm.$valid) {
            // Send information to DB
        }
    }
}